version: "3.8"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: yukkos_web_prod
    restart: always
    ports:
      - "8080:80"
    environment:
      - VITE_API_URL=https://api.yukkos.com
